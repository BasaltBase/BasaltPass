# BasaltPass environment variables
# Copy this file to `.env` at the project root and fill in your secrets.

# JWT signing secret for backend tokens (required in production)
JWT_SECRET=change-me

# Optional: override config.yaml values via environment variables
# Use the BASALTPASS_ prefix and replace dots with underscores.
# Examples:
# BASALTPASS_ENV=production
# BASALTPASS_SERVER_ADDRESS=:8080
# BASALTPASS_DATABASE_DSN=
# BASALTPASS_CORS_ALLOW_ORIGINS=http://localhost:5173,http://localhost:3000

# If your .env is not in the default location, point to it explicitly
# BASALTPASS_ENV_FILE=./.env
