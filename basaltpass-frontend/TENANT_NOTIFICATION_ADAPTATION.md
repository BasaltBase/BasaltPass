# tenantnotificationtranslated API translated

## translated
alreadysuccesstranslatedtenantnotificationmanagementtranslated (`src/pages/tenant/Notifications.tsx`) translatedtotranslatednotification API translated。

## translated

### 1. translated
```typescript
// translated
import {
  TenantNotification,
  TenantCreateNotificationRequest,
  TenantUser,
  createTenantNotification,
  getTenantNotifications,
  deleteTenantNotification,
  getTenantUsers
} from '../../api/tenantNotification';

// updatetranslated
import {
  TenantNotification,
  TenantCreateNotificationRequest,
  TenantUser,
  tenantNotificationApi
} from '../../api/tenant/notification';
```

### 2. API translated
- `getTenantNotifications(params)` → `tenantNotificationApi.getNotifications(page, pageSize)`
- `createTenantNotification(data)` → `tenantNotificationApi.createNotification(data)`
- `deleteTenantNotification(id)` → `tenantNotificationApi.deleteNotification(id)`
- `getTenantUsers(search)` → `tenantNotificationApi.getTenantUsers(search)`

### 3. translated
- translated `receiver_id` translated，translated `user` translatedinfo
- translatedcantranslatedsecuritytranslated（`receiver_ids?`, `app?`, `user?`）
- updatetranslated，translatedusernameandtranslated

### 4. typesecuritytranslated
- translated `receiver_ids` cantranslatedsecuritytranslated
- translatedcantranslated (`?.`) translatednottranslatederror
- translatedsecuritytranslated

## translated

### translated
- translatedhastranslatedusertranslated：translatedusertranslatedandtranslated/email
- translated：translated"translated"translated

### translated
- translatedusertranslated
- translateduser（translated）
- translatedalreadytranslatedusertranslated

### errortranslated
- translatederrortranslated
- networkrequestfailedtranslated
- translatedstatustranslated

## API translated
- getnotificationlist: `GET /api/v1/tenant/notifications`
- createnotification: `POST /api/v1/tenant/notifications`
- deletenotification: `DELETE /api/v1/tenant/notifications/{id}`
- gettenantuser: `GET /api/v1/tenant/notifications/users`

## translatedstatus
- ✅ TypeScript typetranslated
- ✅ translatederroralreadytranslated
- ✅ translated
- ✅ API translatedupdatetranslated

## translated
1. translated API translated，`receiver_ids` iscantranslated，translatedornottranslatedtenanttranslatedhasuser
2. notificationtranslated `user` translatedis `receiver_id` translated
3. appinfotranslated `app` translated，cantranslated undefined
4. translatedhas API translated `tenantNotificationApi` translated，translated

## translated
translatedalreadytranslatedandtranslatedtenanttranslatednotification API translated，cantranslated。
